# Vault Configuration
# Number of Vault replicas to deploy in the HA cluster
# Minimum recommended: 3 for production HA (allows 1 node failure)
# For development: 1 is acceptable
VAULT_REPLICAS=3
KUBE_CONTEXT="docker-desktop"
export TF_VAR_KUBE_CONTEXT="${KUBE_CONTEXT}"